import chai from 'chai';
import ShortUrlController from '../../src/url/shortUrlController';
import config from '../fixtures/config.json';

describe('Short URL Controller', () => {
  it('Should return', () => {
  	chai.assert.typeOf('a', 'string');
  });
});